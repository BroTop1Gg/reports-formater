---
page_numbering: true
header_text: "Іваненко І.І., група ЗП-31"
---

# ЛАБОРАТОРНА РОБОТА № 1

Тема: дослідження алгоритмів сортування та аналіз їх обчислювальної складності.

Мета: набуття практичних навичок щодо реалізації та порівняння основних алгоритмів сортування, визначення їх ефективності для різних обсягів вхідних даних.

# ЗАВДАННЯ

1. Реалізувати алгоритми сортування бульбашкою, вибором та злиттям.
2. Провести заміри часу виконання для масивів різного розміру.
3. Побудувати графіки залежності часу від кількості елементів.
4. Зробити висновки щодо практичної складності кожного алгоритму.

# ХІД РОБОТИ

Для ілюстрації результатів роботи програми було отримано наступний скріншот:

![Рисунок 1.1 — Скріншот роботи програми](placeholder)

Обчислювальна складність алгоритму злиття описується формулою:

$$T(n) = C_1 \cdot n + C_2 \cdot n \log_2 n$$ (1.1)

де $C_1$ та $C_2$ — константи, що залежать від реалізації.

Реалізація головного скрипту обробки наведена нижче.

Лістинг 1.1 — Головний скрипт обробки (src/main.py)
```python
```

Результати замірів часу виконання для різних алгоритмів представлено у таблиці:

Таблиця 1.1 — Порівняння часу сортування (мс)
| Алгоритм    | n=100 | n=1000 | n=10000 |
|-------------|-------|--------|---------|
| Бульбашка   | 0.5   | 48.2   | 4920.1  |
| Вибір       | 0.3   | 32.1   | 3180.5  |
| Злиття      | 0.1   | 1.2    | 15.3    |

# ВИСНОВКИ

У ході лабораторної роботи було реалізовано три алгоритми сортування та проведено порівняльний аналіз їх ефективності. Експериментальні дані підтвердили теоретичні оцінки обчислювальної складності: алгоритм злиття демонструє найкращу продуктивність на великих обсягах даних, тоді як прості алгоритми (бульбашка, вибір) є прийнятними лише для невеликих масивів.

# Додаток А. Повний лістинг коду програми

Цей додаток містить вихідний код реалізованих алгоритмів сортування.

Лістинг А.1 — Алгоритм сортування злиттям (src/main.py)
```python
def merge_sort(arr):
    if len(arr) > 1:
        mid = len(arr) // 2
        L = arr[:mid]
        R = arr[mid:]
        merge_sort(L)
        merge_sort(R)
        i = j = k = 0
        while i < len(L) and j < len(R):
            if L[i] < R[j]:
                arr[k] = L[i]
                i += 1
            else:
                arr[k] = R[j]
                j += 1
            k += 1
        while i < len(L):
            arr[k] = L[i]
            i += 1
            k += 1
        while j < len(R):
            arr[k] = R[j]
            j += 1
            k += 1
```
