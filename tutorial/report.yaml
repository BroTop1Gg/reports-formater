# Paste the YAML generated by the AI here!

# -----------------
# Troubleshooting:
# -----------------
# 1. Did you copy the ```yaml wrapper by mistake? 
#    Delete it! This file must contain ONLY pure YAML code.
#
# 2. Do you see an error like this?
#    "ERROR - Failed to parse YAML: while scanning an alias"
#    This happens if the AI used asterisks (*) instead of 
#    dashes (-) for lists. Please replace them with dashes manually.